<?php
/**
 * Example handcrafted bootstrap script webflow requests.
 *
 * The 'boot.php' script is generated when the plugin is activiated.
 * There may be situation, however, that requires a handcrafted one.
 * This is an example.
 */
if (defined('ABSPATH'))
	require_once ABSPATH.'wp-load.php';
else
	require_once __DIR__.'/../../../../wp-load.php';

require_once __DIR__.'/../vendor/autoload.php';

// vim: set ts=4 noexpandtab fdm=marker syntax=php: ('zR' to unfold all)
